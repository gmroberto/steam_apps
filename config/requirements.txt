# Boilerplate requirements
requests==2.31.0
retry==0.9.2
decorator>=3.4.2
py>=1.4.26

# Environment variables support (optional but recommended)
python-dotenv==1.0.0

# YAML configuration support
PyYAML==6.0.1

# MongoDB insertion script requirements
pymongo==4.6.1

# Airflow dependencies
apache-airflow==2.8.1
apache-airflow-providers-mongo==3.3.0
apache-airflow-providers-http==4.7.0
apache-airflow-providers-json==1.0.0