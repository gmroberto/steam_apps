# Core application dependencies
requests==2.31.0
retry==0.9.2
python-dotenv==1.0.0
PyYAML==6.0.1

# Database dependencies
pymongo==4.6.1
psycopg2-binary==2.9.9

# Airflow core (only what's needed)
apache-airflow==2.11.0